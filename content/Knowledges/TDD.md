---
date: 2024-04-11T17:55:51
updated: 2026-05-15 12:17:48
share: true
noteId: 1778807093426
---
TDD 的核心概念和应用要点是什么？

---

> 在“理解需求，明白架构”是一切工程化软件开发的前提，那么 TDD 就是目前已知的**工程效能最高**的办法

- 程序员的隐性测试成本：
	在平时的“软件开发模式”中，以下两种测试贯穿始终：
	1. **验证性测试**：我们要知道代码有没有错误，一般是“编写一个测试应用，包含大量输出逻辑”，然后“在本地的测试环境中跑一下，并在浏览器中观测结果”。
	2. **定位性测试**：当错误发生时，我们要知道错误发生在哪里，也就是平时所说的打断点 debug。

	但是我们平时都是“**无计划的手动验证**”与“**手动的启发式验证**”，这种都是极其低效的手段。因为这些操作**不可重复**，如果下一个人想要在我的功能上进行编码，他必须花时间完完整整地手动重现之前我验证功能的流程，当然这肯定是不现实的。因此，我们会经常出现“做了 A 功能，结果 B 功能出现问题”，我们急需将它们替换为“**有计划的自动化验证**”和“**有计划的逐模块自动化排查**”，来降低隐性测试的成本。

	TDD 重来不是告诉我们“怎么写测试”、“怎么消除测试人员”、“怎么让开发人员多干一份活”，它是以测试为**里程碑点**的工程化研发过程（**追求功能覆盖率，而非代码行覆盖率**）；同时，测试驱动开发将软件流程中无时无处不在的低效测试手段，以**可重复的、高效的自动化测试代替**，以获得更高的工程效能。

	可以说，**不是程序员要不要写测试，而是不写测试，就等于没干完活**。把自己该干的自动化验证的活，推给下一个人去手动验证，是一种对自己和团队都极其不负责任的事情。
- vibe coding 是完全与 tdd 相反的实践：
	所谓 vibe codeing，就是把需求告诉 ai，让 ai 去实现，如果运行错误，就把控制台的报错，贴给 ai 去修复，然后循环往复。
	这个和我们平时的“跑一下，看看效果，再修改”的 debug 编码模式并没有任何区别，整个软件开发过程中，开发者始终处于 Complicated 认知模式[^1]下，并没有理解业务和代码的过程，很多人在追求 ai 的上输入上下文，但是在开发过程中，如果一个功能需要查询大量的上下文代码，要么是任务分解不到位，要么是对代码库不熟。
- 测试驱动的核心：
	- 深层分析性思考能力的体现，理解需求，分析需求，先产生大致的任务列表，然后在任务列表上进一步分解成可测试的任务。
- 为什么 TDD 具有工程效能：
	- 从团队协作的角度：
 TDD 不光表示我理解了需求，也向其它人**证明**我的确理解了这个需求，同时还有此时的支撑。这是在团队协作中证明员工**靠谱**的基本点。
	- 从长期稳定的角度：
 我们生产效率越来越慢，越来越不敢改动祖传代码。是因为所做的修改一旦出错，就**难以定位究竟是因为什么出的错**。TDD 中由列表任务产生的测试，既是我们实现软件过程的记录，也可以帮助我们发现软件中存在的问题，以及更快地去定位到问题。
	- 从持续提高水平的角度：
 在使用 TDD 开发的过程中，我们对需求和架构会有越来越清晰的认知，而需求和架构也会直接反映到任务列表中。在任务列表中，我们产生了越来却清晰的任务。
- TDD 的整体工作流程：一个简易的例子 [[../Express/restful web service spike|restful web service spike]]
	- 首先将需求分解为**功能点**，也就是将需求转化为一系列**可验证的里程碑点**；
	- 如果已经存在架构或架构愿景，则依据架构中定义的**组件与交互**，将功能点分解为不同的**功能上下文**；
	- 如果尚不存在架构愿景，则可以将功能点作为功能上下文；
	- 将功能点按照功能上下文，分解为**任务项**。也就是进一步将可验证的里程碑点，分解为功能上下文中可验证的任务项。
	- 将任务转化为**自动化测试**，进入红/绿/[[./重构|重构]]循环，驱动功能上下文内的功能实现；
	- 如果重构涉及功能上下文的重新划分，即**提取/合并组建**，即视作对于架构的重构与梳理。需调整后续功能点中对于功能上下文以及任务项的划分。
	- 如此往复，直到所有功能完成。
- TDD 的工程优势：
	- **看测试**降低**判断一个人是否理解了需求的成本**：**写不出测试就是不理解需求**，不理解需求就只能在各种拉通会议中，反复宣讲确认，鸡同鸭讲，消磨精力。
	- **看测试**降低了十分需要进入**探索模式**的成本：在有限的时间（可能是一两天），判断需求是否被正确理解，并通过可管控的工程化方式交付。以此追求**低成本及时止损**。而不是在形成进度阻塞、延期或大量返工的时间，才发现根本原因是**不能确定实现方式**
	- **看功能上下文拆分**来判断**团队是否对架构远景达成了共识**：对于相同的功能点，团队中所有成员拆分出的功能上下文应该也相同。否则所谓架构愿景就是一句空话，或者一堆空话。最终带来的，就是大量独立风格的代码，
	- **看功能上下文拆分**来判断**架构是否能实现当前需求**：如果无法拆分出合理的功能上下文和任务项，那么这个需求，就是当前愿景不易实现的需求。那么就可以通过重构抽取架构要素，演进式调整当前架构。
- 探索合适的与 ai 协作的模式
	![Pasted image 20250419200257](https://knowledge-garden.oss-cn-shanghai.aliyuncs.com/images/Pasted%20image%2020250419200257.png)

[^1]: [[./认知行为模式|认知行为模式]]

## 拆分卡片

- [[../Cards/TDD 用自动化测试替代隐性手动验证成本|TDD 用自动化测试替代隐性手动验证成本]]
- [[../Cards/TDD 是以测试为里程碑的工程化研发过程|TDD 是以测试为里程碑的工程化研发过程]]
- [[../Cards/Vibe coding 与 TDD 的核心差异在于是否先理解和验证需求|Vibe coding 与 TDD 的核心差异在于是否先理解和验证需求]]
- [[../Cards/TDD 通过测试证明开发者理解了需求|TDD 通过测试证明开发者理解了需求]]
