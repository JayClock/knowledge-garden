---
date: 2026-03-26 20:10:47
updated: 2026-07-09 10:45:13
share: true
---

# 全栈工程师简历（两页版）

📞 15000248245 | ✉️ 891853602@qq.com | 📍 上海 | 🎓 上海海事大学 · 软件工程 · 本科 | 28 岁

## 职业摘要

8 年+ 软件/前端开发经验，近 5 年聚焦前端架构、平台工程与全栈协作。主导低代码平台核心渲染引擎、AST 公式引擎、HATEOAS 资源契约消费层等平台型能力建设，擅长将复杂业务规则抽象为 Schema、AST、超媒体契约和本地优先模型文件等可复用工程资产。当前重点围绕 Evidence 探索业务履约建模、AI 辅助建模与可审查交付，关注需求、模型、证据、审查与上线反馈之间的全链路可治理性。

## 核心技能

- **技术基础**：React / TypeScript / Node.js，复杂前端系统设计，前后端资源契约协作
- **架构设计**：配置化交付平台 / HATEOAS 资源契约 / 本地优先模型存储 / AI 辅助建模 / 编译原理（Lexer / Parser / AST）/ 前端安全与沙箱隔离
- **工程化**：Webpack / Babel 调优 / Nx Monorepo / CI/CD / Tauri Desktop / Node.js 中间件与脚手架
- **业务领域**：SaaS（CRM / HRM / ERP）交付平台 / 业务履约建模 / 全链路埋点与异常监控 / AI 工程化交付治理

## 工作经历

| 时间                | 公司           | 职位             |
| ----------------- | ------------ | -------------- |
| 2021.10 ~ 2025.04 | 上海易校信息科技有限公司 | 高级前端专家 / 前端架构师 |
| 2020.06 ~ 2021.03 | 上海子图信息科技有限公司 | 前端工程师          |
| 2016.07 ~ 2020.06 | 上海五五来客技术有限公司 | 前端专家           |

### HATEOAS 资源契约架构

项目简介

此项目聚焦企业级 SaaS 中复杂状态机、多角色、多端协作下的规则漂移问题，目标不是做一层请求封装，而是将散落在 Web、小程序、App 前端中的 `status / role / permission` 判断收敛为后端返回的资源动作契约，让前端从“解释业务规则”转向“消费当前资源状态下的合法动作”。

工作内容与成果

- **资源动作契约消费层设计**：主导前端 HATEOAS 消费层，将当前资源可执行的动作通过 `_links` / `_templates` 与资源状态一起返回，前端基于 relation 与 action 消费契约，降低状态机规则在多端重复实现带来的联动改动与测试回归成本。
- **TypeScript SDK 与类型安全约束**：设计基于 relation 图谱遍历的 TypeScript 客户端 SDK，封装 `follow()` 与 `action()` 等能力，并结合泛型约束与运行时校验，尽量让 relation、action 与 payload 在开发期可提示、运行期可被 zod 统一验证。
- **兜底状态与错误标准化处理**：围绕资源加载失败、动作缺失、契约缺失、链式导航中断等场景，区分资源不可用、动作不可执行、网络错误与鉴权失败等状态，为 UI 层提供一致的禁用、隐藏、重试与错误反馈策略。
- **Agent 友好与权责对等**： 动态注册 Agent 可调用工具，意图识别不会超出当前用户权限。基于 template 构建 self-Heal Loop 的 Payload 参数生成，避免出现 HTTP 400 参数错误。

## 项目经历

### 低代码工作流引擎

项目简介

设计并开发内部工作流引擎，在支撑传统业务工作流之上支持 AI Agent 工作流扩展，解决内部低成本落地大模型应用的难题。

工作内容与成果

-  **引擎架构设计**：自主设计并实现了基于 **DAG (有向无环图)** 的业务工作流执行引擎，利用 **Kahn 算法** 实现节点拓扑排序与循环检测，支持复杂审批流与自动化业务逻辑编排。
- **核心模块开发**：设计了基于 **注册模式**和**策略模式**的节点/组件扩展系统，实现了 Start、Form（动态表单）、HTTP、Condition 等核心节点的执行器，遵循开闭原则，方便后续业务组件库的低成本扩充。
- **变量编辑器与数据总线构建**：基于 **Tiptap/ProseMirror** 自研了低代码动态变量编辑器，支持通过 `/` 或 `${}` 快捷唤起全局/上游节点变量菜单；设计了自研的表达式引擎，在运行时精准解析节点间的依赖引用，实现复杂业务场景下的跨节点参数传递与上下文状态管理。
- **高性能状态同步**：在 NestJS 中实现了 **SSE (Server-Sent Events) / WebSocket** 实时通信机制，支持长耗时业务节点（如批量数据处理、跨系统接口调用）的执行状态实时监控与日志流式推送，优化了开发者的调试体验。
- **工程化治理**：采用 **Monorepo** 架构集中管理前后端代码，通过 Docker 多阶段构建优化容器镜像体积，实现了从开发到生产环境的全链路 CI/CD 自动化部署。

### 全链路埋点与监控 SDK

项目简介

此埋点 SDK 面向轻流平台的前端数据采集与异常观测场景，目标不是做大而全的数据平台，而是沉淀一套低侵入、高可靠、可降级的前端 SDK，让业务事件、曝光事件、接口异常与公共上下文能够稳定回流到产品、研发、测试与运营反馈链路中。

工作内容与成果

- **SDK 架构与插件化设计**：设计并实现了基于“核心包 + 适配包”的监控 SDK 架构，通过插件化机制支持多前端框架（React、Vue、Vanilla JS）无缝集成，并支持业务方高度自定义监控项与功能扩展。
- **核心性能与异常采集**：构建了全方位的性能与异常数据采集方案，利用底层 API 结合全局事件拦截，全面覆盖 FCP、LCP、CLS 等核心 Web Vitals 性能指标以及复杂的 JavaScript 运行期异常。
- **SaaS 平台搭建与大盘可视化**：利用 React 与 shadcn/ui 搭建了现代化的高颜值 SaaS 监控控制台，结合底层 ClickHouse 列式存储的查询优势，实现了海量监控数据的多维度可视化展示与实时大盘监控，为业务决策提供了精准的数据支撑。

### Evidence｜本地优先的业务履约建模与 AI 辅助建模平台

项目简介

此项目面向复杂业务履约建模场景，参考 Obsidian 的 Vault、Canvas 与 Properties/Bases 思路，将合同、履约请求、履约确认、参与方、角色和上下文沉淀为可版本化的 `.evidence` 模型文件，并通过 React Flow Canvas、Rust Axum API、Tauri Desktop 与 AI 建模助手形成“本地文件资产 + 可视化图谱 + 可审查 AI 提案”的建模工作流。

工作内容与成果

- **本地优先模型资产设计**：设计 `.evidence/entities` 与 `.evidence/associations` 文件结构，将 Evidence、Participant、Role、Context 等业务概念及关系落为可读 YAML，支持 Git Diff、模型审查与 AI 工具读取，避免关键业务知识只沉淀在数据库或图形状态中。
- **领域语义 Canvas 与自动布局**：基于 React Flow / ELK 实现履约建模画布，围绕 RFP → Proposal → Contract → Fulfillment Request → Fulfillment Confirmation 的阶段关系，完成合约前上下文、合约上下文、履约泳道、角色层和参与对象层的自动布局。
- **Rust 本地运行时与文件投影层**：基于 Axum 构建后端服务，将 `.evidence` YAML 文件投影为 Workspace、Diagram、LogicalEntity、Relationship 等领域资源；桌面端通过 Tauri 启动内嵌 API 与 SQLite，本地工作区可独立运行，Web 与 Desktop 共用同一套 React 前端。
- **AI 辅助建模与协同治理**：集成 AI SDK、SSE 与 Pi RPC，将自然语言需求流式转化为建模 proposal，展示 reasoning、tool call、tool execution 与 JSON 变更摘要；将多智能体协同能力收敛为 Evidence 的建模加速器，并保留人工审查与回滚边界。

## 自我评价

- 习惯从业务问题、系统边界与协作链路出发理解技术方案，不把前端工作局限在“页面实现”，而是更关注需求如何被建模、验证、交付与回流。
- 擅长将低代码 Schema、AST 规则、HATEOAS 契约、埋点协议、`.evidence` 模型文件、Trace / Evidence 等复杂问题沉淀为可复用、可测试、可审查的工程资产，降低跨团队协作中的知识损耗与规则漂移。

---

## 投递微调指南

- **投前端架构**：前置 Evidence + HATEOAS，突出 Canvas 自动布局、资源契约消费层和 Web/Desktop 单前端架构
- **投 AI 工程化**：前置 Evidence，突出 AI proposal-first、SSE 事件流、工具边界、人工审查与可回滚能力
- **投全栈 / 平台工程**：保持 Evidence + 低代码工作流 + HATEOAS，强化 Rust Axum、Tauri Desktop、本地优先模型存储与契约测试

## Evidence 项目扩展素材

- 详见 [[./Evidence 项目简历描述与面试话术|Evidence 项目简历描述与面试话术]]：包含 1 分钟/3 分钟面试介绍、Obsidian 功能映射、追问回答与按投递方向裁剪的版本。