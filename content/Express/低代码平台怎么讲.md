---
tags:
  - frontend-interview
  - low-code
  - oral
  - 前端面试系列
created: 2026-03-24
date: 2026-03-24 13:23:09
updated: 2026-03-28 16:02:23
share: true
---

# 低代码平台怎么讲

## 一句话先定性

> 我不会把低代码平台讲成“一个拖拖拽拽的页面编辑器”，而会讲成“用稳定的数据模型和运行时机制，把页面搭建和业务联动产品化”。

---

## 30 秒版本

> 如果面试里让我讲低代码平台，我会先讲清楚它不是单纯的搭页面工具，而是一套把页面结构、组件物料、事件编排和数据联动统一起来的系统。前端在里面做的也不只是 UI，而是协议设计、编排引擎、渲染机制、状态联动和扩展体系。真正的难点不是拖拽本身，而是怎么让这套系统既能扩展，又能在复杂业务里保持稳定。

---

## 1 分钟版本

> 低代码平台这个题，我一般不会从界面编辑器开始讲，而是先讲它的本质。对我来说，低代码平台本质上是两件事：第一，用一套稳定的数据结构去描述页面和组件；第二，用一套运行机制去承接这些数据，让页面、事件和数据联动真的跑起来。
>
> 所以它不是“拖一个按钮上去”这么简单，而是后面要有 schema、组件注册机制、渲染引擎、事件编排、全局变量和数据绑定这些东西一起配合。前端在这里做的，也不仅是编辑器交互，还包括协议设计、运行时设计和可扩展性设计。
>
> 如果让我总结低代码平台最值得讲的点，我会说不是页面搭得多快，而是你能不能把复杂业务抽成一套稳定的模型，并且让这套模型在编辑态和运行态都成立。

---

## 2 到 3 分钟版本

> 我理解低代码平台，不是一个“所见即所得”的拖拽工具，而是一套把页面搭建能力、配置能力和业务联动能力工程化的系统。
>
> 它的核心不在于有没有一个可视化画布，而在于你有没有一套稳定的数据模型去描述页面。比如页面上有哪些组件、组件之间怎么嵌套、组件属性怎么存、事件和数据源怎么挂接。这些东西如果没有统一 schema，后面就很难做版本管理、回滚、多人协作和运行时渲染。
>
> 所以我通常会把低代码平台拆成几层来看。第一层是协议层，也就是 schema / DSL，定义组件树、属性、事件和数据源。第二层是物料层，也就是组件注册中心，负责把 type 映射到真正的渲染器和配置面板。第三层是编排层，也就是编辑器和拖拽引擎，负责插入、移动、删除和布局调整。第四层是运行时层，负责把 schema 真的渲染出来，并处理事件流和数据联动。
>
> 真正难的地方在两块。第一块是数据模型要足够稳定，因为整个系统都压在它上面。第二块是编辑态和运行态的边界要清楚。编辑态需要选中、高亮、拖拽、配置面板；运行态只关心真实业务逻辑和页面表现。如果这两层混在一起，系统会越来越难维护。
>
> 所以如果让我讲低代码平台，我会重点讲“协议、编排、运行时”这三件事，而不是只讲拖拽交互。因为拖拽只是入口，真正决定平台能不能做大的，是背后的模型和机制。

---

## 如果面试官追问“低代码最难的点是什么”

> 我觉得最难的不是把页面拖出来，而是怎么把复杂业务抽象成平台可以承接的稳定模型。因为平台一旦做通用化，就很容易在扩展性和复杂度之间失衡。模型太松，系统会乱；模型太死，业务又接不住。

---

## 如果面试官追问“前端在低代码里最有价值的地方是什么”

> 我觉得前端在低代码平台里的价值，不是单点页面开发，而是把页面能力、状态能力和运行机制抽象成平台能力。也就是说，前端做的是一套能不断复用和扩展的基础设施，而不是一张业务页面。

---

## 最后一句收尾

> 所以低代码平台真正能体现能力的地方，不是做了一个编辑器，而是能不能把页面、事件、数据和运行时统一成一套稳定系统。

## 按项目经历风格优化后的写法

### 项目名称

轻流低代码平台

### 项目简介

面向企业移动办公、CRM、HRM、CMS、ERP 等通用 SaaS 场景建设低代码平台，重点解决复杂业务表单、数据表格、仪表盘配置与多业务模块复用的问题。平台以 schema 驱动为核心，统一表单、表格、仪表盘和权限等基础能力，支撑业务快速搭建与持续扩展。

### 工作内容与成果

☻ 主导通用表单及复杂表单逻辑处理的 schema 设计，基于插件化机制实现插拔式表单项、校验规则与扩展能力接入，提升复杂业务表单的复用性与可维护性

☻ 主导通用表格体系设计，以及表格与表单之间交互规范的抽象沉淀，实现围绕同一份 JSON/schema 的配置驱动，减少多处重复定义和前端硬编码

☻ 主导完成仪表盘编辑器与渲染引擎开发，结合 d3 等技术实现定制图表能力，让业务侧可以通过配置快速搭建数据看板，提升数据展示与业务分析效率

☻ 参与系统权限体系设计、多租户用户体系设计，支撑通用 SaaS 场景下的账号、组织、角色与数据权限隔离

☻ 引入 Nx 落地公司内 monorepo 项目集群，推动统一规范、统一文档和统一业务口径建设，降低循环依赖和重复建设问题，提升多团队协作效率

### 一段简历压缩版

参与轻流低代码平台核心能力建设，围绕表单 schema、通用表格、仪表盘编辑器与渲染引擎、多租户权限体系等方向推进平台化抽象。通过插件化表单机制、统一 JSON 配置模型和 Nx monorepo 工程治理，提升复杂业务场景下的复用效率、扩展能力与团队协作一致性。

### 更像你当前简历风格的三条式写法

- 主导复杂表单 schema 与插件化机制设计，支持表单项、校验规则和扩展能力按配置插拔，提升通用 SaaS 场景下的表单复用效率。
- 主导通用表格与表单交互规范抽象，以及仪表盘编辑器和渲染引擎开发，实现基于统一 JSON/schema 的配置驱动和数据展示能力。
- 参与多租户权限体系设计，并引入 Nx 建设 monorepo 工程体系，推动统一规范、文档和业务口径，减少循环依赖和重复建设。

### Boss / 猎聘项目经历描述

负责轻流低代码平台核心能力建设，面向 CRM、HRM、CMS、ERP 等通用 SaaS 场景，推进表单 schema、通用表格、仪表盘编辑器与渲染引擎、多租户权限体系等基础能力平台化。通过插件化表单机制、统一 JSON/schema 配置模型以及 Nx monorepo 工程治理，提升复杂业务配置效率、跨模块复用能力和团队协作一致性。

### Boss / 猎聘三条式写法

- 主导复杂表单 schema、通用表格规范与仪表盘编辑器/渲染引擎建设，支撑低代码平台在多类 SaaS 业务场景下快速配置与复用。
- 基于插件化机制实现表单项、规则与扩展能力插拔，结合统一 JSON/schema 模型减少前端重复开发和硬编码逻辑。
- 参与多租户权限体系设计，并引入 Nx 建设 monorepo 工程体系，提升平台治理能力和多团队协作效率。

## 关联逐字稿

- [[./埋点 SDK 和低代码编辑器 15 分钟逐字稿|埋点 SDK 和低代码编辑器 15 分钟逐字稿]]

## 相关追问

- [[./低代码面试反问与陷阱题|低代码面试反问与陷阱题]]
