---
date: 2025-05-04T21:44:17
updated: 2025-05-10T22:24:33
share: true
---
- **HTTP/0.9 - 单行协议**
	- 只有 GET 请求行，无请求头和请求体
	- 只能传输 HTML 文件，以 ASCII 字符流返回
	- 无响应头
- **HTTP/1.0 - 多类型支持**
	- 支持多种文件类型，不限于 ASCII 编码
	- 引入请求头和响应头（key-value 形式）
	- 每个请求都需要建立新的 [[./TCP|TCP]] 连接
- **HTTP/1.1 - 持久连接**
	- 引入持久连接( keep-alive )：一个 [[./TCP|TCP]] 连接可传输多个 HTTP 请求
	- 默认开启 keep-alive，通常限制 6-8 个并发连接
	- 存在队头阻塞问题：前面的请求阻塞会影响后续请求
	- 引入 Host 字段，支持虚拟主机
	- 引入 Chunk transfer 机制处理动态内容长度
- **HTTP/2.0 - 多路复用**
	- 一个域名只使用一个 [[./TCP|TCP]] 长连接
	- 引入二进制分帧层，实现多路复用
	- 可对请求设置优先级
	- 引入 HTTPS(HTTP + TLS) 加密
- **HTTP/3.0 - QUIC 协议**
	- 基于 UDP 协议而非 [[./TCP|TCP]]
	- 实现了类似 [[./TCP|TCP]] 的流量控制和可靠传输
	- 集成 TLS 加密
	- 实现多路复用
	- 解决 [[./TCP|TCP]] 队头阻塞问题