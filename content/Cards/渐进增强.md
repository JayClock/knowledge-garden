---
date: 2024-10-12T11:45:27
updated: 2025-06-06T20:42:34
share: true
tags:
  - review
---
当我们通过 [[../Knowledges/输入 URL 到页面展示，发生了什么|URI 访问的页面]] 时。页面中会关联 CSS、JavaScript 等所需的资源，并包含其他页面的链接。浏览器会下载对应的 CSS、JavaScript，完成界面的渲染。你也可以通过页面中的链接，去访问其他关联的页面。

在整个过程中，我们从来无需考虑 CSS、JavaScript 来自哪里，关联的页面是否来自同一个网站(因为不关心，也因此导致了 [[./XSS 攻击|XSS 攻击]])。因为从始至终，我们都将这些关联的资源看作是一个完整的资源在消费。这是我们习以为常的场景。

从系统集成的角度上来看待这个过程。**网页中对其他资源的引用，实际上表示了对其他服务计算结果的整合**。一个典型的例子就是 Google Analytics，虽然它是以 js 的形式出现。但当页面引用到了这个 js 资源时，实际上完成的是对 Google Analytics 服务的整合。

而点击页面中包含的链接时，浏览器会为你请求对应的网站资源，并将计算结果（大概率是另一个页面）在浏览器客户端里展示。**这也可以看作是对其他系统的集成**。更具体地讲，也就是浏览器客户端通过延迟（Lazy）调用的方式，整合了其他系统提供的服务。

所以无论是 css、js 这样的资源，还是通过超链接引用的其他页面，都是服务端返回的页面中描述的。而客户端则根据服务端提供的描述，选择恰当的时机，访问这些关联的资源，也就是整合其他系统提供的服务。这样的系统集成方式，我们称之**为由服务器指导的客户端侧集成（Server-side guided client-side integration）。**

由于对于关联资源的访问是由客户端发起的，并不是由服务器端强制的，那么客户端可能出于某些原因（安全、计算资源受限等）选择不去访问某些关联资源。

比如，在智能手机大规模流行之前，偷菜游戏流行之时，大部分手机上的浏览器并不具备运行完整偷菜游戏的逻辑， 那时的主流做法是，浏览器不加载任何复杂的游戏样式，而是只保留文字与基本交互代码，用户通过点击文字来进行操作（变成了文字游戏）。

从这样的角度出发，我们就会发现，CSS、JavaScript 或者页面中通过超链接引用的其他页面，都可以看作是对当前页面的增强。比如 CSS 增强了当前页面的视觉效果，js 增强了当前页面的交互，页面中的超链接为页面中的内容提供了额外的信息。

这种按需索取，而不要求消费全部关联的服务。也正是渐进式的消费方式，促使互联网成为了一个异构、松耦合且开放的系统。同时这也正是我们希望自己所构建的系统能够具有的特质。

而它在前端开发领域最主要的应用，就是渐进增强（Progressive Enhancement）。

渐进增强作为一种前端开发策略，提出了“内容为核心，表现和行为分层叠加”的模型，确保所有用户都能访问基本内容，同时允许高性能设备获得更丰富的体验。为现代前端架构设计，尤其是开发框架（如PWA和React懒加载等）提供了理论指导。
1. **核心内容优先**
	- **基本功能**：首先确保网页的核心内容和功能在所有浏览器中都能正常工作。这意味着使用最基本的 HTML 和 CSS 来构建页面的骨架（现在都是框架配合 webpack），确保内容可以被所有用户访问。
	- **语义化 HTML**：使用语义化标签，如 `<header>`、`<nav>`、`<article>` 等，来描述页面的结构，而不是依赖样式或 JavaScript
2. **逐步增强**
	- **JS 增强页面功能**：调通过分层的方式为用户提供核心功能，同时逐步加载非关键内容，从而确保不同设备和网络条件下的最佳体验。这一理念在React、Vue和Angular等主流前端框架中得到了不同程度的实践和支持。
		- React**通过`React.lazy`和`Suspense`组件实现了懒加载机制**，这是一种按需加载组件或模块的技术。开发者可以使用`React.lazy()`动态导入组件，并通过`Suspense`定义加载过程中的占位内容。例如，在单页应用（SPA）中，高分辨率图片或复杂数据表单仅在用户滚动到特定部分时才被加载，这显著提升了用户体验
		- Vue通过**defineAsyncComponent函数支持异步组件的定义**，允许开发者仅在需要时从服务器加载组件，从而减少初始页面加载时间并优化性能结合懒加载机制，Vue能够在低性能设备上显著提升用户体验。例如，当一个组件仅在用户滚动到其所在区域后才被完全激活时，系统能够节省宝贵的计算资源
		- Angular**通过路由系统实现了模块按需加载**（Lazy Loading），显著减少了初始加载时间并改善了用户体验。例如，在创建新项目时，开发者可以通过`ng generate module`命令生成独立的特性模块，并在路由配置文件中使用`loadChildren`动态导入模块。这种方法不仅将应用拆分为更小的代码块，还允许仅加载用户访问特定路径时所需的功能模块。对于大型复杂应用，这种优化效果尤为显著，有助于提升性能。
	- **CSS 增强页面样式**：跨平台兼容性与优雅降级
		- 利用CSS特性查询(如@supports规则)为支持现代特性的浏览器添加高级样式。例如,针对支持Flexbox的浏览器,可以通过display: flex优化布局,而不支持该特性的浏览器则回退到基础样式。这种分层设计能够确保网站在各种设备上保持一致性,同时避免了因过度依赖特定CSS特性而导致的兼容性问题
		- 媒体查询(@media)。通过根据屏幕尺寸动态 调整布局,开发者可以在不同设备上提供一致的用户体验。例如,针对小屏幕设备,可以 简化导航菜单的设计,而在大屏幕设备上则展示更复杂的布局。这种方法不仅符合渐进增 强的理念,还能显著改善多设备环境下的可用性
	- JSON 增强内容
	-  **缓存设计**：基础内容优先缓存
		- 将 HTML 页面设置为短缓存时间（如 `Cache-Control: max-age=60`），保证内容及时更新。
		- 静态资源（CSS、JS、图片）设置长缓存时间（如 `max-age=31536000`），减少请求。
		- 这样即使 JS 加载失败，HTML 和 CSS 仍可缓存并呈现基础页面。

**一个案例**：一个新闻类 PWA 应用，比如 RSS 订阅工具 follow 也是类似于下面这种形式
- 所有文章内容以 HTML 形式提供（基础层）
- 使用 Service Worker 缓存首页和常用文章（缓存优先）
- 图片、视频、评论组件懒加载（增强层）
- 用户阅读记录保存在 IndexedDB 中（本地状态）
**缓存策略**：

| 资源类型    | 缓存方式            | 是否渐进增强     |
| ------- | --------------- | ---------- |
| HTML 页面 | HTTP 缓存 + SW 缓存 | ✅ 是（基础内容）  |
| CSS 样式  | 强缓存             | ✅ 是（表现增强）  |
| JS 脚本   | 按需加载 + SW 缓存    | ✅ 是（交互增强）  |
| 图片/视频   | 懒加载 + SW 缓存     | ✅ 是（媒体增强）  |
| 用户状态    | IndexedDB       | ✅ 是（个性化增强） |
 **用户体验**：
- **第一次访问**：完整加载所有资源，SW 注册并缓存。
- **后续访问**：即使离线，用户也能看到首页和已缓存的文章。
- **有网络时**：自动更新内容，加载增强功能（如评论、分享按钮）。

**渐进增强和缓存优势**

| 优势           | 描述                   |
| ------------ | -------------------- |
| 🚀 提升首屏性能    | 基础内容优先加载，不依赖复杂逻辑     |
| 🔌 离线可用性     | 即使断网，核心功能仍可使用        |
| 💪 更好地应对弱网环境 | 缓存减少请求次数，增强体验更流畅     |
| 🧱 松耦合架构     | 不同层级可独立更新、缓存、维护      |
| 🧠 提升 SEO    | HTML 内容优先加载，利于搜索引擎抓取 |
| 🔐 安全性增强     | 缓存策略限制不必要的外部请求，降低攻击面 |